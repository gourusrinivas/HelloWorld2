pipeline {
    agent none
    stages {
        stage('Build) {
            steps {
               sh 'python -m py_compile hw2.py'
               stash(name: 'compiled-results', includes: '*.py*')
               }
           }
      }
   }   
           
      
